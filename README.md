# 편의점 야간근무 🏪

> 새벽 3시, 혼자 남았다.

브라우저에서 바로 플레이하는 **랜덤 디펜스 웹게임**. 랜덤 유닛을 뽑고, 합성하고, 새벽 편의점으로 몰려오는 손님(과 이상한 사건)을 계산대 앞에서 막아라.

- 장르: 랜덤 디펜스 · 캐주얼 전략 · 로그라이크 · 밈 코미디
- 플랫폼: 웹 (PC 마우스 / 모바일 터치, 가로·세로 반응형)
- 기술: Vite + React + TypeScript, Canvas 2D, WebAudio (외부 에셋 0개, 라이브러리 최소)

---

## 1. 실행 방법

```bash
npm install
npm run dev        # http://localhost:5173
```

## 2. 개발 서버

```bash
npm run dev        # 로컬 + LAN 노출 (--host). 휴대폰에서 같은 와이파이로 접속해 테스트 가능
```

## 3. 빌드

```bash
npm run build      # 타입체크 후 dist/ 생성
npm run preview    # dist/ 를 로컬에서 미리보기 (http://localhost:4173)
```

기타 스크립트:

```bash
npm run typecheck                      # tsc --noEmit
npm run sim -- 20 sellCommons          # 헤드리스 밸런스 시뮬레이션 (20판, 판매 전략)
npx tsx scripts/playtest.ts http://localhost:4173 shots landscape 60   # 브라우저 자동 플레이 + 스크린샷
npx tsx scripts/playtest2.ts http://localhost:4173 shots               # 게임오버/저장/재시작/상점 흐름 검증
npx tsx scripts/renderSprites.ts out.png src/game/render/sprites/*.ts # 스프라이트 시트 확인
```

Playwright 스크립트는 `npx playwright install chromium` 이 필요하다 (또는 `CHROME_PATH=/path/to/chrome` 지정).
브라우저 테스트가 필요 없으면 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install` 로 설치 시간을 아낄 수 있다.

## 4. 배포 방법

정적 파일이므로 어디든 올릴 수 있다. `vite.config.ts` 의 `base: './'` 덕분에 하위 경로에서도 동작한다.

- **Vercel / Netlify**: 저장소 연결 → Build command `npm run build`, Output `dist`. 끝.
- **GitHub Pages**: `npm run build` 후 `dist/` 를 `gh-pages` 브랜치로 푸시 (`npx gh-pages -d dist`).
- **Cloudflare Pages** (아래 §4-1 상세).
- **직접 호스팅**: `dist/` 폴더를 nginx/S3 등 아무 정적 서버에 업로드.

### 4-1. Cloudflare Pages 배포 (권장, 무료)

**방법 A — GitHub 연동 (푸시하면 자동 배포)**

1. https://dash.cloudflare.com → **Workers & Pages** → **Create** → **Pages** → **Connect to Git**
2. 이 저장소(`djkdb/zunran`)와 브랜치를 선택
3. 빌드 설정:
   - Framework preset: **Vite**
   - Build command: `npm run build`
   - Build output directory: `dist`
   - 환경 변수(Environment variables)에 추가: `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD` = `1` (테스트용 Playwright 가 브라우저를 내려받지 않게 해 빌드 시간을 줄인다), `NODE_VERSION` = `20`
4. **Save and Deploy** → 1~2분 뒤 `https://<프로젝트명>.pages.dev` 주소가 생긴다. 이후 브랜치에 푸시할 때마다 자동 재배포된다.

**방법 B — 명령어 한 줄 (Wrangler)**

```bash
npx wrangler login                 # 브라우저에서 Cloudflare 계정 승인 (최초 1회)
npm run deploy:cf                  # build 후 dist/ 를 Pages 프로젝트 cvs-night-shift 로 업로드
```

처음 실행하면 프로젝트를 새로 만들지 물어보니 Enter. 출력되는 `https://cvs-night-shift.pages.dev` 가 배포 주소다.

### 4-2. 글로벌 랭킹 서버 붙이기 (선택)

랭킹판은 Cloudflare Pages Functions + KV 로 동작한다. KV 를 바인딩하지 않아도 게임은 그대로
돌아가고, 랭킹 탭만 "서버 미연결"로 표시된다.

1. 대시보드 → **Storage & Databases → KV → Create namespace** (이름 아무거나)
2. Pages 프로젝트 → **Settings → Bindings → Add → KV namespace**
   - Variable name: `RANK_KV` (이 이름이어야 한다)
   - Production / Preview 양쪽에 건다
3. 재배포. `functions/` 는 저장소 루트에 있어 Pages 가 자동으로 잡는다.

자세한 구조·검증 규칙·개인정보 범위는 `docs/RANKING.md` 참고.

**커스텀 도메인**: Pages 프로젝트 → **Custom domains** → 도메인 입력. Cloudflare 에서 관리하는 도메인이면 DNS 가 자동으로 잡힌다.

**확인 사항**: `vite.config.ts` 의 `base: './'` 덕분에 추가 설정 없이 동작한다. SPA 라우팅이 없으므로 `_redirects` 도 필요 없다. HTTPS 는 기본 제공되어 클립보드 복사(결과 공유) 기능도 정상 동작한다.

친구에게 링크만 보내면 설치 없이 바로 플레이된다. 모바일은 "홈 화면에 추가"로 앱처럼 실행 가능.

## 5. 주요 파일 설명

```
src/
  App.tsx                      화면 전환(시작/게임/게임오버), 저장 데이터 갱신, 메타 상점 구매
  styles.css                   전역 스타일 + 가로/세로 반응형 레이아웃 + 배너 애니메이션
  game/
    types.ts                   모든 공용 타입 (GameState, Unit, Enemy, EventDef, UISnapshot …)
    config.ts                  상수: 필드/경로/슬롯 좌표, 확률, 비용, 티어·체력 스케일 공식, 시계
    data/units.ts              유닛 15종 정의 (능력치·스킬·오라·대사)
    data/enemies.ts            손님 22종 + 보스 4종 정의 (행동 패턴·대사·면역)
    data/waves.ts              웨이브 생성기 (개체 수, 해금 풀, 새벽 3시 러시, 보스)
    data/events.ts             랜덤 이벤트 28종 (희귀 사건 5종 포함)
    data/rewards.ts            웨이브 보상 카드 19종
    data/dialogue.ts           밈 대사, 뽑기 멘트, 보스 인트로, 게임오버 멘트, 팁
    data/achievements.ts       업적 40종 정의 + 판정 (조건은 순수 함수)
    data/missions.ts           오늘의 미션 16종 + 추가 조건 6종
    data/dailyChallenges.ts    ZUNRAN DAILY 규칙 10종 (기존 Modifiers 위에 얹는 값 묶음)
    data/runTitles.ts          런 제목 생성 규칙 (플레이 기록 기반)
    daily.ts                   날짜 seed → 오늘의 규칙·미션 (같은 날짜면 전 세계 동일)
    engine/Engine.ts           엔진 본체: tick(고정 스텝), dispatch(액션), FX 큐, UI 스냅샷
    engine/waveSystem.ts       웨이브 타이머/스폰 큐/클리어 보너스
    engine/enemySystem.ts      손님 이동·행동 AI·상태이상·보스 패턴·계산대 도달 피해
    engine/unitSystem.ts       오라 버프·타겟팅·공격·스킬·투사체
    engine/mergeSystem.ts      합성 (70% 강화 / 25% 상위 희귀도 / 5% 특수)
    engine/eventSystem.ts      랜덤 이벤트 발생/만료/전역 배율 합산
    engine/rewardSystem.ts     보상 3택 생성·적용, 판 전체 영구 강화(PermaBuffs)
    engine/skillSystem.ts      긴급 스킬(셔터·폐기 처리)과 쿨다운
    engine/economy.ts          코인·처치 보상·통계
    engine/rng.ts              시드 RNG (시뮬레이션 재현용)
    render/Renderer.ts         Canvas 렌더링 (매장 배경, 유닛/손님, 투사체, 파티클, 말풍선, 흔들림, 정전/비)
    render/sprites/*.ts        16x16 픽셀 스프라이트 데이터 (문자열 그리드)
    render/sprites.ts          스프라이트 레지스트리 + 래스터 캐시
    audio/sfx.ts               WebAudio 합성 효과음 20종 + 루프 BGM (긴장 모드)
    save/storage.ts            localStorage 저장/로드 + v1 → v3 마이그레이션, 용량 정리
    rank/types.ts              랭킹 기록 타입 + 정렬 기준 (클라이언트·서버 공용)
    rank/validate.ts           기록 검증 (서버가 쓰는 순수 함수, 게임 상수 기반)
    rank/board.ts              보드 정렬·삽입·순위 계산 (서버·테스트 공용)
    rank/api.ts                랭킹 API 호출 (실패해도 게임을 막지 않는다)
    rank/payload.ts            한 판 결과 → 전송 payload
    save/meta.ts               메타 업그레이드 6종 정의, 야간 수당 계산
    save/stats.ts              한 판 기록 → 누적 도감 통계 병합, 패배 원인 분석
  ui/
    Icon.tsx                   SVG 아이콘 세트 + 티어 눈금 (이모지 대체)
    useGame.ts                 Engine ↔ React 브리지 (rAF 루프, 10Hz 스냅샷, FX → 배너/사운드)
    GameScreen.tsx / Hud.tsx / BottomPanel.tsx / Banner.tsx
    RewardOverlay.tsx          웨이브 보상 3택 화면
    StartScreen.tsx            시작 화면 + ZUNRAN DAILY 카드 + 하단 고정 탭 6종
    CodexScreen.tsx            도감 (유닛/손님별 누적 통계, 특징, 관련 사건)
    AchievementsScreen.tsx     업적 (달성률, 그룹 필터, 숨겨진 업적)
    HistoryScreen.tsx          최근 근무 기록 30판
    GameOverScreen.tsx         영업 종료 화면 + 업적 토스트
    RunReport.tsx              근무 보고서 (런 제목, MVP, 패배 원인, 사건, 미션 결과)
    Certificate.tsx            근무 인증서 (Canvas 렌더 → Web Share / 클립보드 / 이미지 저장)
    RankScreen.tsx             랭킹판 (오늘의 근무 / 전체 최고 / 내 기록)
    NicknameField.tsx          랭킹 표시 이름 입력 (시작·게임오버·랭킹 탭 공용)
functions/
  api/rank/[board].ts          GET 랭킹 조회 (Cloudflare Pages Function)
  api/rank/submit.ts           POST 기록 제출 (검증 후 KV 저장)
  api/rank/rename.ts           POST 표시 이름 변경 (기록은 그대로)
  api/rank/_shared.ts          KV 접근 + 응답 헬퍼
scripts/
  sim.ts                       헤드리스 밸런스 시뮬레이터 (전략 4종, 데일리 규칙 지정 가능)
  pacing.ts                    초반 긴장감·템포 측정 (첫 피해 웨이브, 한 판 길이)
  playtest.ts                  Playwright 자동 플레이테스트
  playtest-expansion.mjs       확장 기능 종단 테스트 19항목
  playdebug.mjs                플레이어 관점 회귀 테스트 (레이아웃 넘침·잘림·터치 타깃)
  walkthrough.mjs              신규 유저 첫 판을 장면별로 촬영 (문서·소개용 18컷)
  titles.ts                    런 제목 분포 + 종합 밸런스 지표
  checkSprites.ts / renderSprites.ts   스프라이트 검증/렌더
docs/DESIGN.md                 아키텍처·데이터 구조·시스템 설계 문서
docs/RANKING.md                랭킹 서버 구조·설치·검증 범위·개인정보
```

## 6. 게임 시스템 설명

**핵심 루프**: 뽑기(코인) → 배치 → 자동 공격 → 손님 웨이브 → **보상 3택 1** → 같은 유닛 3개 합성 → 더 강한 손님 → 보스 → 생존 시간 기록.

- **웨이브 보상 3택 1 (로그라이크)**: 3웨이브마다, 그리고 보스 직후에 게임이 멈추고 카드 3장 중 하나를 고른다. 19종이 있고 한 판 동안 유지된다. 공격력·공격속도·사거리·코인·치명타 같은 누적 강화, 유닛 즉시 지급, 티어 상승, 체력 회복, 무료 뽑기, 그리고 "새벽 장사"(다음 웨이브 손님 2배·코인 2배) 같은 도박 카드까지. 같은 카드를 또 고르면 다음부터 등장 확률이 낮아진다.
- **긴급 스킬 2종**: [셔터 내려](45초 쿨) 손님 전원 2.5초 정지 + 뒤로 밀기, [폐기 처리](70초 쿨) 화면 전체 피해. 위기 탈출용이자 손이 심심하지 않게 하는 조작.
- **코너 배치 보너스**: 진열대 줄마다 특성이 다르다. 음료 코너 사거리 +18, 과자 코너 공격속도 +12%, 라면 코너 공격력 +22%. 어느 줄에 둘지가 전략이 된다.
- **연속 처치 콤보**: 2.2초 안에 이어서 처치하면 콤보가 쌓이고 5연쇄마다 보너스 코인이 터진다 (최대 4배).
- **밤이 깊어질수록 빨라진다**: 초반 16초에서 시작해 10웨이브에 22초로 벌어졌다가, 이후 14초까지 다시 줄어든다.

- **필드**: 640×640 논리 좌표의 편의점. 손님은 입구(좌상) → 음료 코너 → 과자 코너 → 라면 코너 → 계산대(S자 경로)로 이동. 계산대 도달 시 편의점 체력 감소. 진열대 3줄 × 7칸 = 21 슬롯에 유닛 배치.
- **뽑기**: 100원 시작, 뽑을 때마다 +10원(최대 420). 일반 60 / 희귀 28 / 에픽 10 / 전설 2%. 뽑은 유닛은 빈 슬롯에 자동 배치. 유닛 탭 → 빈 칸 탭 = 이동, 다른 유닛 탭 = 교환.
- **유닛 15종**: 일반(삼각김밥·야간 알바생·라면 진열대·냉장고), 희귀(포스기·커피머신·CCTV·청소기·아이스크림 냉동고), 에픽(전자레인지·로또 기계·택배 접수기), 전설(점장), 특수·합성 전용(편의점 고양이·10년차 알바). 각 유닛은 공격 방식/타겟팅/명중 효과/오라/스킬을 가진다.
- **합성**: 같은 유닛·같은 티어 3개 → 70% 티어+1, 25% 상위 희귀도 랜덤 유닛(같은 티어), 5% 특수(점장 또는 특수 유닛). 티어당 피해 ×3.4. 최대 5티어.
- **손님 22종 + 보스 4종**: 웨이브 1부터 24까지 거의 매 웨이브 새 손님이 하나씩 열린다. 기본(1)/뛰는 손님(2·아주 빠르고 잘 새어 나간다)/담배(3·빠름)/술취한(4·비틀거림)/가격(5·멈춰서 뒤를 막음)/라면(6·라면 코너에서 머물며 강해짐)/배달기사(7)/단체(8)/술 취한 친구들(9·뭉쳐 이동)/ATM(10·멈춰서 코인을 훔침)/봉투(11·처치 시 봉투값 요구)/커플(12·둘이 서로 회복)/새벽 3시 진상(13·WARNING)/충전기(14·유닛 마비)/택배(15·탱커)/화장실(16·맞으면 가속)/사장님 불러(17·주변 가속)/유령(18·깜빡임)/야자 끝난 고딩(19·3~5명 무리)/라이브 켠 손님(20·주변에 보호막)/좀비(22)/배달 오토바이(24·감속·넉백 면역). 보스: 10 폐기 직전 도시락, 20 새벽 3시 단체 손님, 30 본사 직원, 40 월말 매출 마감 (이후 순환).
- **시작 자원**: 코인 200원(뽑기 1~2회). 첫 웨이브부터 방어가 빠듯해서 2~5웨이브에 뛰는 손님이 한두 명 지나가며 체력이 깎인다 — "계산대에 닿으면 깎인다"를 말이 아니라 손해로 배운다.
- **웨이브**: 기본은 22초에서 웨이브마다 0.22초씩 짧아진다(최소 14초, 보스 36초). 다만 아직 위협이 없는 1~10웨이브는 기다리는 시간이 되므로 6초에서 0초까지 선형으로 깎아 16초부터 시작한다. 1~3웨이브는 학습용. 웨이브 13 = 03:00 "새벽 3시입니다" 20명 러시. 체력 스케일 `1 + 0.16w + 0.05w²`(40 이후 ×1.07/웨이브), 계산대 도달 피해도 웨이브마다 6%씩 증가. 웨이브 시작마다 시급(40+9w원) 지급, 웨이브 클리어(손님 전원 처리) 시 보너스.
- **랜덤 이벤트 23종**: 웨이브 3부터 32~48초마다. 손님 몰림/정전/비/1+1 행사/본사 점검/배달 폭주/사장님 전화/길고양이 등.
- **연출**: LEGENDARY 풀스크린 배너 + 흔들림 + 플래시, WARNING 배너, 보스 인트로, NEW RECORD, 로또 1등, 손님 말풍선(밈 대사), 정전 시 손전등 효과, 비 오는 연출.
- **업적 40종**: 진행·수집·기록·사건·밈 5개 그룹. 밈 업적 10종은 숨겨져 있어 달성 전까지 제목과 설명이 `???` 로 나온다("전자레인지에 모든 것을 맡겼다", "새벽 3시를 버티지 못했다", "진상에게 졌다"…). 달성 순간 게임오버 화면에 배너와 전용 효과음이 하나씩 순서대로 뜬다.
- **ZUNRAN DAILY**: 날짜를 seed 로 매일 하나씩 돌아가는 특별 규칙 10종(음료의 밤, 정전, 폐기 대란, 손님 폭주, 점장님 없는 날…). 새 시스템이 아니라 기존 `Modifiers`·뽑기 확률·웨이브 생성에 곱해지는 값 묶음이라 밸런스 검증도 기존 시뮬레이터로 한다.
- **TODAY'S MISSION**: 같은 날짜면 전 세계가 같은 미션을 받는다. 주 목표 16종 + 추가 조건 6종이 조합되고, 깨면 야간 수당을 하루 한 번 추가 지급한다.
- **근무 보고서**: 게임오버 화면이 통계 나열에서 보고서로 바뀐다. 플레이 기록에서 고른 **런 제목**(「새벽 3시의 악몽」「합성 중독」「고양이가 출근했다」), MVP, **패배 원인**(누가 몇 번 들어왔는지·누가 제일 아팠는지·결정적인 순간·남은 코인), 그날 발생한 사건 목록, 미션 결과, 새로 딴 업적.
- **근무 인증서**: Canvas 로 그려 Web Share API 로 공유하거나(모바일), 클립보드에 이미지로 복사하거나, PNG 로 저장한다. 외부 서버도 라이브러리도 쓰지 않는다.
- **도감 강화**: 유닛은 획득/MVP/합성/최고 티어/총 피해, 손님은 발견/처치/통과/받은 피해/한 판 최다 처치가 누적된다. 카드를 누르면 특징 한 줄과 관련 사건이 펼쳐진다.
- **최근 근무 기록**: 최근 30판을 런 제목·웨이브·MVP·데일리 여부와 함께 보관한다.
- **밤의 단계**: 22시 → 자정 → 새벽 2시 → 새벽 3시로 화면 색조가 단계적으로 어두워지고, 3시 이후에는 형광등이 가끔 깜빡인다. 02:00 에 전조 배너가 먼저 뜬다.
- **자동 정리**: 칸 21개가 다 차면 뽑기가 막히고 → 같은 유닛 3개가 안 모이고 → 합성이 멈춘다. 그래서 칸이 다 찼을 때만, 짝이 없는 1티어 유닛을 **한 번에 하나씩** 판다(기본 켜짐, 끌 수 있음). 짝이 있는 유닛은 절대 건드리지 않으므로 합성 재료가 사라질 일이 없다. 칸이 찼는데 자동 정리를 꺼 뒀다면 뽑기 버튼이 `SLOT FULL · 정리하세요` 로 바뀌고 정리 줄이 깜빡인다.
- **메타**: 한 판 종료 시 야간 수당(`웨이브×14 + 처치×0.25 + 코인×0.004`). 시작 코인/초기 체력/뽑기 할인/희귀 확률/에픽·전설 확률/코인 획득량 각 5레벨, 전부 사는 데 약 11판. 다 사면 사망 웨이브 중앙값이 29 → 33 으로 오르고 하한이 15 → 25 로 올라간다.
- **글로벌 랭킹**: 판이 끝나면 기록이 자동으로 올라간다. 보드는 둘 — **오늘의 근무**(ZUNRAN DAILY 규칙, 그날 모두가 같은 조건)와 **전체 최고**(일반 근무). 순위는 웨이브 → 소요 시간 → 처치 수 순이고, 한 사람당 최고 기록 하나만 남는다. **내 기록** 탭은 서버 없이도 기기 기록으로 동작한다. 이름을 정하기 전에는 기록을 올리지 않고 보류했다가, 게임오버 화면에서 이름을 받으면 그때 전송한다. 나중에 이름을 바꾸면 이미 올라간 기록의 이름도 함께 갱신된다(기록 숫자는 그대로). 서버에는 표시 이름·기록 숫자·기기 식별자 앞 8자만 올라가고, 랭킹 등록을 끄면 아무것도 보내지 않는다. 기록은 서버에서 검증한다 — 웨이브 대비 최소 소요 시간, 등장 가능한 손님 수, 콤보 ≤ 처치, 보스 처치 상한, 뽑기 대비 합성 수, 제출 간격. 다만 게임을 실제로 재생해 보는 검증은 아니라서 "그럴듯한 범위 안의 조작"까지는 막지 못한다.
- **저장**: `localStorage["cvs-night-shift:v1"]` 에 최고 웨이브/최장 생존/총 플레이/도감/메타/음소거/랭킹 이름.
- **비주얼 방향 (아케이드 스타일)**: 셸도 픽셀로 만든 네온 아케이드. 짙은 보라 바탕(`#120e24`) 위에 민트·핑크·골드 세 네온만 쓰고, 테두리는 항상 3px에 모서리를 3px 깎아 계단으로 만든다(배경 이미지 링 기법). 상태는 테두리 색으로만 말한다 — 기본 EDGE / 준비됨 MINT / 행동·위험 PINK / 돈·보상 GOLD. 라운드 0, 그라데이션·블러·글로우·이모지 없음. 막대는 칸으로 끊는다(웨이브 5칸·체력 10칸·보스 20칸). 글꼴은 Do Hyeon(제목·유닛 이름) + Silkscreen(숫자·영문 상태 라벨) + Gothic A1(본문). 필드 바탕도 같은 보랏빛 야간 톤으로 맞춰 껍데기와 픽셀 매장이 한 화면으로 읽힌다. 시안과 구성 요소 규격은 `design/` 참고.
- **조작**: 
  - 유닛을 **끌어서** 다른 칸에 놓거나 교환할 수 있고, **탭-탭**(유닛 탭 → 빈 칸 탭)으로도 옮길 수 있다.
  - 뽑은 유닛은 자동으로 선택되어 빈 칸이 강조된다 — 한 번 탭으로 원하는 코너에 배치한다.
  - 유닛을 선택하면 같은 종류·같은 티어 유닛이 필드에서 함께 빛나 합성 재료 위치가 한눈에 보인다.
  - 희귀 이상이거나 2티어 이상 유닛 판매는 한 번 더 확인한다.
  - 키보드 `D` 뽑기, `Space` 일시정지, `Esc` 선택 해제. ×2 배속, 자동 합성 토글, 정리 버튼.
  - 일시정지 화면에서 자동 합성·자동 정리를 켜고 끄고, [근무 끝내고 나가기]로 언제든 퇴근할 수 있다. 되돌릴 수 없으니 두 번 눌러야 하고, 퇴근해도 지금까지 기록이 저장되고 야간 수당을 받는다.
  - 하단 탭(시작·강화·랭킹·도감·업적·기록)은 스크롤과 무관하게 화면 아래에 붙어 있다. 살 수 있는 강화가 있으면 강화 탭에 점이 찍히고, 시작 화면 기록 칸을 누르면 해당 탭으로 바로 간다.
  - 첫 판에는 상황에 맞는 코치 힌트가 뜬다(뽑기 → 배치 → 합성 → 긴급 스킬 → 보스 예고). 게임오버 화면에서 결과를 클립보드로 복사해 공유할 수 있다.

**밸런스 검증 (헤드리스 시뮬레이션, 20시드)**: 사람에 가까운 전략(보상은 등급 높은 카드 선택, 몰리면 긴급 스킬) 기준 사망 웨이브 중앙값 43 (최소 24), 합성을 안 하면 중앙값 24, 뽑기만 남발하면 33, 메타 업그레이드 만렙이면 최대 75까지. 20웨이브부터 체력이 조금씩 깎이기 시작해 40웨이브 보스가 분명한 벽이 된다.

## 7. 추후 추가하면 좋은 기능

- 유닛 도감에서 상세 능력치/스킬 열람, 손님 도감 대사 모음
- 데일리 시드 모드 ("오늘의 야간근무" — 모두 같은 랜덤 시드로 경쟁)
- 도전 모드: "전설 금지", "일반 유닛만", "냉장고 챌린지" 등 릴스용 제약 모드
- 유닛 스킨/편의점 브랜드 테마, 계절 이벤트 손님
- 튜토리얼 힌트 강화(첫 판 한정 말풍선), 설정(진동/그래픽 품질)
- 리플레이 저장(시드 + 입력 로그) → 영상 자동 촬영
- PWA 매니페스트/서비스워커로 오프라인 플레이 + 홈 화면 설치

## 8. 릴스 제작에 가장 좋은 플레이 장면 10개

세로(9:16) 모바일 화면이 기본 촬영 구도다. 콘솔에서 `__game.engine` 으로 상태를 조작해 장면을 연출할 수 있다.

1. **첫 뽑기에서 전설** — 풀스크린 `LEGENDARY` + "점장이 출근했습니다." + 화면 흔들림·금색 플래시. (`__game.engine.state.rng.next = () => 0` 후 뽑기)
2. **새벽 3시입니다** — 웨이브 13 시작 시 붉은 배너와 20명 러시 + WARNING 진상 등장. 화면이 손님으로 가득 찬다.
3. **체력 1%에서 보스 처치** — 붉은 비네팅 펄스 + 보스 체력바 + "처리 완료" 배너. (`state.hp = 1`)
4. **합성 ????? 특수 결과** — 합성 시 5% 로 편의점 고양이/10년차 알바가 튀어나오는 순간의 배너·폭발 파티클.
5. **로또 1등!!!** — 로또 기계 2% 치명타 25배 피해. 금색 배너 + 큰 숫자 플로터.
6. **점장 등장으로 전황 역전** — 계산대 앞까지 밀린 상태에서 점장 잔소리 스킬("야! 정신 안 차려?!")로 한 방에 정리.
7. **정전 이벤트** — 화면이 어두워지고 유닛 주변만 손전등처럼 밝은 상태에서 손님이 밀려오는 장면.
8. **월말 매출 마감 보스** — 40웨이브, 보호막 페이즈 + 코인이 줄어드는 연출, "숫자가 맞지 않는다".
9. **냉장고/CCTV만으로 버티기** — "편의점에서 가장 쓸모없는 물건만 뽑아봤습니다" 컨셉. 공격 유닛 없이 감속 오라만으로 손님이 기어가는 화면.
10. **NEW RECORD + 영업 종료 통계 카드** — 신기록 배너 후 "이번 야간근무에서 처리한 손님: N명", MVP 유닛, 야간 수당 획득까지 한 컷.

보너스: 술 취한 손님이 뒤로 비틀거리며 "사장님… 사랑해요" 말풍선, 봉투 손님이 죽으면서 "봉투값 20원 주세요", 좀비 손님 "으어어… 컵라면…" 등 밈 말풍선 모음 컷.
