import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.djkdb.zunran',
  appName: '편의점 야간근무',
  webDir: 'dist',
  backgroundColor: '#120e24',
  ios: { contentInset: 'never' },
};
export default config;
